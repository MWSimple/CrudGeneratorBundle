{% block extends %}
{{ "{% extends 'MWSimpleCrudGeneratorBundle::layout.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block title %}" }}
{{ "{{ parent() }} - {{ 'views.edit.edit'|trans({'%entity%': '" }}{{ entity }}{{ "'}, 'MWSimpleCrudGeneratorBundle') }}" }}
{{ "{% endblock %}" }}

{{ "{% block page %}" }}
    <h1>{{ "{{ 'views.edit.edit'|trans({'%entity%': '" }}{{ entity }}{{ "'}, 'MWSimpleCrudGeneratorBundle') }}" }}</h1>

    {{ "{% form_theme edit_form 'MWSimpleCrudGeneratorBundle:widget:fields.html.twig' %}" }}
    
    <div class="panel panel-default center-block">
        <div class="panel-heading">
            <h3 class="panel-title center-block">
                {{ "{{ 'views.new.creation'|trans({'%entity%': '" }}{{ entity }}{{ "'}, 'MWSimpleCrudGeneratorBundle') }}" }}
            </h3>
        </div>
            
        <div class="panel-body">
            {{ '{{ form(edit_form) }}' }}
        </div>
    </div>


    {% set hide_edit, hide_delete = true, false %}
    {% include 'crud/views/others/record_actions.html.twig.twig' %}
{{ "{% endblock %}" }}
{% endblock body %}
    
{{ "{% block javascript %}"}}
<script type="text/javascript">
$('input[type="checkbox"].style2').checkbox({
    buttonStyle: 'btn btn-base',
    buttonStyleChecked: 'btn btn-success',
    checkedClass: 'glyphicon glyphicon-check',
    uncheckedClass: 'glyphicon glyphicon-unchecked'
});
</script>
{{ "{% endblock %}" }}       
    
