{% block extends %}
{{ "{% extends 'MWSimpleCrudGeneratorBundle::layout.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block title %}" }}
{{ "{{ parent() }} - {{ 'views.new.creation'|trans({'%entity%': '" }}{{ entity }}{{ "'}, 'MWSimpleCrudGeneratorBundle') }}" }}
{{ "{% endblock %}" }}

{{ "{% block page %}" }}
    
    
    {{ "{% form_theme form 'MWSimpleCrudGeneratorBundle:widget:fields.html.twig' %}" }}
    <div class="panel panel-default col-lg-8 center-block">
        <div class="panel-heading">
            <h3 class="panel-title center-block">
                {{ "{{ 'views.new.creation'|trans({'%entity%': '" }}{{ entity }}{{ "'}, 'MWSimpleCrudGeneratorBundle') }}" }}
            </h3>
        </div>
            
        <div class="panel-body">
            {{ '{{ form(form) }}' }}
        </div>
    </div>
            

    

    {% set hide_edit, hide_delete = true, true %}
    {% include 'crud/views/others/record_actions.html.twig.twig' %}
{{ "{% endblock %}" }}
{% endblock body %}
    
{{ "{% block javascript %}"}}
<script type="text/javascript">
$('input[type="checkbox"].style2').checkbox({
    buttonStyle: 'btn btn-base',
    buttonStyleChecked: 'btn btn-success',
    checkedClass: 'glyphicon glyphicon-check',
    uncheckedClass: 'glyphicon glyphicon-unchecked'
});
</script>
{{ "{% endblock %}" }}       
